# Dockerfile for [Anarchy Golf](http://golf.shinh.org/)

## setup

```
$ git clone git@github.com:inaniwa3/Dockerfile-for-Anarchy-Golf.git
$ cd Dockerfile-for-Anarchy-Golf
$ docker-compose build
$ docker-compose run --rm caddy
# caddy update
```
